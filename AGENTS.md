# AnotherRedisDesktopManager 项目规则

## 项目说明

这是从 GitHub 开源仓库 fork 后的个人定制版本，当前主要包含：

- 连接分组能力。
- 连接列表排序与分组排序。
- 页面样式与交互细节调整。
- 连接测试、代理支持、安全相关的小改动。

## 协作约定

- 默认使用简体中文沟通。
- 修改时优先保持原项目的 Vue 2、Element UI、现有组件结构和代码风格。
- 优先做小范围、可回退的改动，避免顺手重构无关模块。
- 不要主动执行编译、打包、测试、启动开发服务等验证命令。
- 修改完成后只说明改了哪些文件、影响范围和需要用户自行验证的点。
- 用户会自行进行运行、编译和测试验证。

## 前端风格方向

- 整体偏简约、克制、实用，不做过重的卡片化、阴影、浮动动效。
- 左侧连接列表优先保持信息密度，减少大圆角、大间距和多色装饰。
- 浅色/深色主题都要保留可读性。
- 图标、按钮、hover 状态应尽量统一为中性色，只有明确状态信息才使用强调色。

## 注意事项

- 连接、分组、排序相关逻辑集中在 `src/components/Connections.vue`、`src/components/ConnectionWrapper.vue`、`src/components/ConnectionMenu.vue`，修改时要避免破坏拖拽排序、展开折叠和连接打开逻辑。
- Redis 数据操作相关功能要谨慎处理，涉及删除、清空、导入或执行命令时，应优先确认现有安全限制，不要扩大危险操作入口。
